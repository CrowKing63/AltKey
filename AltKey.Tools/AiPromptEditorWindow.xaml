<Window x:Class="AltKey.Tools.AiPromptEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 프롬프트 편집기"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SettingsBg}"
        KeyboardNavigation.TabNavigation="Cycle"
        AutomationProperties.Name="AI 프롬프트 편집기 창">
    <Border Margin="14"
            Padding="16"
            Background="{DynamicResource SettingsBg}"
            BorderBrush="{DynamicResource SettingsBorder}"
            BorderThickness="1"
            CornerRadius="12">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Text="AI 프롬프트 편집기"
                   AutomationProperties.Name="AI 프롬프트 편집기 제목"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="기본 AI 프롬프트를 아래 창에서 편집하고 저장하세요."
                       AutomationProperties.Name="AI 프롬프트 편집 안내"/>
            <TextBlock Text="저장하면 메인 앱이 설정 파일을 다시 읽어 즉시 최신 프롬프트를 사용합니다."
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       AutomationProperties.Name="AI 프롬프트 저장 안내"/>
        </StackPanel>

        <TextBox x:Name="PromptTextBox"
                 Grid.Row="2"
                 Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 TextWrapping="Wrap"
                 Padding="10,8"
                 FontSize="14"
                 KeyboardNavigation.TabIndex="0"
                 AutomationProperties.Name="AI 기본 프롬프트 입력"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="저장"
                    Width="96"
                    Height="34"
                    Margin="0,0,8,0"
                    KeyboardNavigation.TabIndex="1"
                    Click="OnSave"
                    AutomationProperties.Name="AI 프롬프트 저장 버튼"/>
            <Button Content="닫기 (Esc)"
                    Width="120"
                    Height="34"
                    KeyboardNavigation.TabIndex="2"
                    Click="OnClose"
                    AutomationProperties.Name="AI 프롬프트 닫기 버튼"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
