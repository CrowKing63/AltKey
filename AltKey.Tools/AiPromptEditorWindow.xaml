<Window x:Class="AltKey.Tools.AiPromptEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 프롬프트 편집기"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SettingsBg}"
        KeyboardNavigation.TabNavigation="Cycle"
        AutomationProperties.Name="AI 프롬프트 편집기 창">
    <Window.Resources>
        <!-- 접근성: 긴 텍스트 편집 시 포커스와 입력 영역 경계가 분명하게 보이도록 공용 스타일을 적용합니다. -->
        <Style TargetType="Button" BasedOn="{StaticResource FluentButtonStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource FluentTextBoxStyle}"/>
    </Window.Resources>

    <Border Margin="14"
            Padding="16"
            Background="{DynamicResource SettingsBg}"
            BorderBrush="{DynamicResource SettingsBorder}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,10">
                <TextBlock Margin="0"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Text="AI 프롬프트 편집기"
                           AutomationProperties.Name="AI 프롬프트 편집기 제목"/>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="기본 AI 프롬프트를 아래 입력창에서 편집한 뒤 저장하세요."
                               TextWrapping="Wrap"
                               AutomationProperties.Name="AI 프롬프트 편집 안내"/>
                    <TextBlock Text="저장하면 메인 앱이 설정 파일을 다시 불러와 최신 프롬프트를 사용합니다."
                               Margin="0,4,0,0"
                               Foreground="{DynamicResource SettingsFgSub}"
                               TextWrapping="Wrap"
                               AutomationProperties.Name="AI 프롬프트 저장 반영 안내"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource FluentCardBorderStyle}">
                <TextBox x:Name="PromptTextBox"
                         Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         TextWrapping="Wrap"
                         Padding="12,10"
                         FontSize="14"
                         KeyboardNavigation.TabIndex="0"
                         AutomationProperties.Name="AI 기본 프롬프트 입력"/>
            </Border>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">
                <Button Content="저장"
                        Width="96"
                        Height="34"
                        Margin="0,0,8,0"
                        KeyboardNavigation.TabIndex="1"
                        Click="OnSave"
                        AutomationProperties.Name="AI 프롬프트 저장 버튼"/>
                <Button Content="닫기 (Esc)"
                        Width="120"
                        Height="34"
                        KeyboardNavigation.TabIndex="2"
                        Click="OnClose"
                        AutomationProperties.Name="AI 프롬프트 닫기 버튼"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
