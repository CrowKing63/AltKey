<Window x:Class="AltKey.Tools.ProfileMappingReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="프로필 매핑 편집 검토"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        KeyboardNavigation.TabNavigation="Cycle"
        AutomationProperties.Name="프로필 매핑 편집 검토 창">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"
                   Text="프로필 매핑 편집 검토"
                   AutomationProperties.Name="검토 창 제목"/>

        <TextBlock Grid.Row="1"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"
                   Text="태스크 5의 목적은 구현이 아니라 검토입니다. 현재 매핑 구조를 점검하고, 별도 편집 도구로의 분리 적합성을 판단합니다."
                   AutomationProperties.Name="검토 목적 안내"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,8">
                <TextBlock Text="현재 구성 요약" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding SummaryText}" AutomationProperties.Name="현재 구성 요약 값"/>
                <TextBlock Text="{Binding RiskText}" AutomationProperties.Name="리스크 요약 값"/>
            </StackPanel>

            <DataGrid x:Name="ReviewGrid"
                      Grid.Row="1"
                      ItemsSource="{Binding Entries}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      KeyboardNavigation.TabIndex="0"
                      AutomationProperties.Name="프로필 매핑 목록">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="프로세스 이름" Binding="{Binding ProcessName}" Width="2*" />
                    <DataGridTextColumn Header="레이아웃 이름" Binding="{Binding LayoutName}" Width="2*" />
                    <DataGridTextColumn Header="검토 상태" Binding="{Binding Status}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="2" Margin="0,10,0,0" Padding="10" BorderThickness="1" BorderBrush="#FFCCCCCC" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="판단 결과" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding DecisionText}" TextWrapping="Wrap" AutomationProperties.Name="2단계 후보 판단 결과"/>
                </StackPanel>
            </Border>
        </Grid>

        <Button Grid.Row="3"
                Content="닫기 (Esc)"
                Width="120"
                Height="34"
                HorizontalAlignment="Right"
                Margin="0,12,0,0"
                KeyboardNavigation.TabIndex="1"
                Click="OnClose"
                AutomationProperties.Name="검토 창 닫기 버튼"/>
    </Grid>
</Window>
